#include "horario.h"
#include "process.h"
#define TRUE 1
#define FALSE 0

int main(void){
    while(TRUE){
        char *s;
        s = (char*) malloc(sizeof(char) * 8);
        scanf("%s" , s);
    }

    return 0;
}